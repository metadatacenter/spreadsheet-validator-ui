import { Select, styled } from '@mui/material';

const BaseSelect = styled(Select)({
  borderRadius: '20px',
  paddingTop: '10px',
  paddingLeft: '40px',
  paddingBottom: '10px',
  paddingRight: '40px',
  margin: '10px',
});

export default BaseSelect;
